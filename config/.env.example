# =============================================================================
# QuranBot Environment Configuration Template
# =============================================================================
# Copy this file to config/.env and fill in your actual values
# 
# IMPORTANT: Never commit your actual .env file to version control!
# =============================================================================

# Discord Bot Configuration
# Get your bot token from: https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here

# Your Discord server (guild) ID
# Enable Developer Mode in Discord, right-click your server, Copy ID
GUILD_ID=your_guild_id_here

# Audio Configuration
# FFmpeg path (adjust based on your operating system)
FFMPEG_PATH=/usr/bin/ffmpeg                    # Linux
# FFMPEG_PATH=/opt/homebrew/bin/ffmpeg         # macOS with Homebrew
# FFMPEG_PATH=C:\ffmpeg\bin\ffmpeg.exe        # Windows

# Optional: Bot Customization
BOT_NAME=QuranBot
BOT_VERSION=1.0.0

# Optional: Logging Configuration
LOG_LEVEL=INFO                                  # DEBUG, INFO, WARNING, ERROR
LOG_TO_DISCORD=true                            # Send important logs to Discord

# Optional: Quiz Configuration
QUIZ_TIMEOUT=30                                 # Seconds to answer quiz questions
DAILY_VERSE_TIME=06:00                         # Time for daily verse (24-hour format)

# Optional: Audio Configuration
DEFAULT_RECITER=Saad Al Ghamdi                 # Default reciter name
AUDIO_BITRATE=128                              # Audio quality (64, 128, 192, 320)
VOLUME_LEVEL=0.5                               # Default volume (0.0 to 1.0)

# =============================================================================
# Setup Instructions:
# 
# 1. Copy this file: cp config/.env.example config/.env
# 2. Edit config/.env with your actual values
# 3. Make sure config/.env is in your .gitignore (it already is!)
# 4. Install FFmpeg on your system if not already installed
# 5. Run the bot: python main.py
# ============================================================================= 